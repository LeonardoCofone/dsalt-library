"""
Setup script for DSALT package.

This setup.py is kept for compatibility but pyproject.toml is the primary configuration.
"""
from setuptools import setup

# Metadata is now in pyproject.toml
setup()